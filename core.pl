%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Terms (algebra)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Terms:
%   0, 1, x, y, z, ...
%   plus(A,B)   for A + B
%   times(A,B)  for A * B
%   neg(A)      for -A

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Formulas (logic)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Atoms:
%   prop(P)              propositional atom P
%   eq(T1,T2)            equation T1 = T2
%   pred(Name, Args...)  generic predicate

% Connectives:
%   and(P,Q)
%   or(P,Q)
%   not(P)
%   implies(P,Q)

% Quantifiers:
%   forall(X, P)
%   exists(X, P)
